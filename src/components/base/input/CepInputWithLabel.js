import React from 'react'

import { InputWithLabel } from '../'

const CepInputWithLabel = (props) => {
    return (
        <InputWithLabel
            { ...props }
        />
    )
}

export default CepInputWithLabel